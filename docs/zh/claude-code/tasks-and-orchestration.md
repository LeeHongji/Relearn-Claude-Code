# 任务与编排

当 agent 系统开始出现后台执行、长流程、委派与协作时，“只有聊天记录”就不够了，必须引入显式任务模型。

## 建议对照的源码位置

- `src/tasks.ts`
- `src/utils/tasks.ts`
- `src/components/tasks/*`

## 这部分真正重要的点

任务模型让系统可以把工作表达成可跟踪、可展示进度、可后台执行、可协作的结构。
